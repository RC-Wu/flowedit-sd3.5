experiment:
  name: sd35_3d_smoke_base
  mode: stub
  benchmark: 3d_smoke

model:
  family: sd3.5
  checkpoint: TODO/path/to/sd3.5.ckpt

diagnose:
  enabled: true
  minimal_grid: true
  report_only: true

runtime:
  device: cuda
  mixed_precision: fp16
